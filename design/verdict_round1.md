# Round 01 — Verdict
대상 시안: variant-a2-toss.html (토스 블루 컬러 실험)
판정일: 2026-07-14
판정자: heommm (Claude 피드백 반영)

## KEEP — 다음 라운드에서 그대로 유지
- spine(경로선) + 스크롤 연동 fill 애니메이션
  이유: "길을 걷는다" 컨셉과 정확히 맞물리는 이번 라운드의 시그니처 요소. 손대지 말 것.
- 모노 유틸 라벨(eyebrow / code 텍스트) 스타일
  이유: 법률·행정 서비스에 신뢰감을 더하는 디테일. 유지.
- bignum(지배 숫자) + sparkline 근거 섹션 (Evidence)
  이유: "재현 그래프" 라벨 등 카피 규칙을 지키면서 통계를 강하게 전달함.
- manifesto 섹션 카피 톤
  이유: 단정 금지 + 근거 병기 규칙을 지키면서도 힘 있는 문장. 그대로.
- Q&A 아코디언, footer 고지 문구
  이유: 기능·카피 모두 문제 없음. 변경 불필요.

## KILL — 다음 라운드에서 제거/교체
- news 카드 3열 균일 그리드 (동일 테두리·동일 크기 카드 3개)
  이유: "everything-is-a-card" 안티패턴. AI-generated처럼 보이는 핵심 원인 중 하나.
  대안 방향: hairline 구분선 리스트, 또는 비대칭(큰 헤드라인 1 + 작은 2) 레이아웃으로 교체.

## REMIX — 아이디어는 유지, 다음 라운드에서 변형
- 히어로 섹션
  현재: 순수 타이포그래피만 존재, 시각적 초점(anchor) 없음.
  변형 방향: 카피·구조는 유지하되 제품 아티팩트(위임장/신청서 목업, 또는 spine 다이어그램 축소판)를
  시각 앵커로 추가. 스톡 이미지·AI 렌더 금지(카피 규칙 §5 준수).

- gold 색 배분
  현재: 120만원 하이라이트, station 04(term) dot 2곳에만 소극적으로 사용.
  변형 방향: 반복되는 악센트 규칙으로 확장 — 핵심 숫자, CTA 밑줄 등. "의도된 시스템"처럼
  보이도록 최소 3회 이상 규칙적으로 반복.

- 섹션 배경 리듬 (흰색 ↔ soft-gray 반복)
  현재: 전체적으로 평평함, 대비되는 순간이 없음.
  변형 방향: manifesto 또는 final CTA 섹션 중 1곳을 full-bleed navy 밴드로 승격.
  주의: wrapper 재구조 필요(기존에 v3로 미뤄둔 작업, §4 디자인 시스템 참고).

- stations 리스트 (로드맵 미리보기, station 04 "잠금" 카드)
  현재: opacity만 낮춰 잠금 상태 표현 — 임팩트 약함.
  변형 방향: 잠금 상태를 더 명확히 드러내는 시각 처리(예: 자물쇠 아이콘 대신 블러/그라데이션 마스크 등)
  탐색. 단, §4 이모지 최소화 원칙은 유지.

## 다음 라운드 지시 (Claude Code용)
- round-01의 KEEP 항목은 코드 그대로 재사용(재생성 금지, 토큰 절약).
- KILL 항목(news 카드)은 완전히 새 레이아웃으로 교체.
- REMIX 4개 항목만 집중적으로 변주하여 이번엔 시안 2~3개만 생성(전체 재생성 아님).
- 디자인 토큰(§4)·카피 규칙(§5) 엄수, 단정 표현 금지는 기존과 동일하게 적용.
